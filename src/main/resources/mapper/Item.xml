<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.paiyuan.wjx.mapper.ItemMapper">
    <insert id="itemAdd">
        insert into hl_wenjuanxing(activity, name, sojumpparm, joinid, timetaken, submittime, totalvalue, sign)
        values
        (#{activity}, #{name}, #{sojumpparm}, #{joinid}, #{timetaken}, #{submittime}, #{totalvalue}, #{sign})
    </insert>
</mapper>